# Direct dependency versions verified by this repository's CI and local test
# suite. Keep backend/pyproject.toml as the compatibility declaration; pass
# this file to pip when a repeatable development or CI environment is wanted:
#
#   python -m pip install -c backend/constraints.txt -e "backend[dev]"
#
# This intentionally constrains the declared runtime/dev dependencies and the
# Cochl SDK's audio dependency, rather than pretending that a platform-neutral
# fully hashed lock can cover every uvicorn[standard] wheel.
cochl==2.0.0
fastapi==0.136.3
google-cloud-storage==3.12.1
httpx==0.28.1
httpx2==2.5.0
pydantic==2.13.4
pytest==9.0.3
python-dotenv==1.2.2
python-multipart==0.0.32
soundfile==0.14.0
starlette==1.3.1
uvicorn==0.49.0
